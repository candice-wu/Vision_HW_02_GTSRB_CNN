## 1. 核心推論信賴度過濾與安全攔截實作

- [x] 1.1 修改 `gtsrb.py` 中的單張圖片推論流程，為 CNN+STN 模型預測結果實作 0.75 信心分數門檻判定。若 `cnn_conf.item() < 0.75`，將原本類別顯示攔截改為顯示黃橘色警告訊息：「這是非德國交通號誌 (信賴度過低)」。
- [x] 1.2 在 `gtsrb.py` 中，為支援 `predict_proba()` 的傳統機器學習模型 (RF, KNN, AdaBoost, NN) 導入信賴度提取與 0.75 門檻判定。若預測的最大機率值小於 `0.75`，亦進行攔截並顯示「這是非德國交通號誌」警告字樣。
- [x] 1.3 為 SVM 模型之決策距離 `decision_function` 實作信心轉換與 0.75 閥值安全過濾（例如以 Sigmoid 將決策分數轉換為 0.0 ~ 1.0 的近似機率信賴度，並於低於 0.75 時判定為未知號誌並攔截）。

## 2. 測試與學術驗證

- [x] 2.1 本機啟動 Streamlit 伺服器並上傳一張非德國號誌圖片（例如台灣號誌或隨機圖片），驗證各個模型在推論此類 OOD 圖片時，其信賴度是否被精確判定且被安全攔截。
- [x] 2.2 上傳一張高解析度的真實德國交通號誌，驗證當預測置信度高於 0.75 時，系統是否能正常輸出其預測 Class 數值以確保可用性。
- [x] 2.3 確認雙階段歷史評估頁籤 (`tab2` 與 `tab3`) 的指標圖表載入完全正常，且測試集評估腳本 (`src/evaluate.py`) 能正常執行不產生干擾。
